.card
	.card-header
		.fa.fa-user
		= user.email
		-# = link_to user.email, user_path(user)
	-# .card-body
	-# 	= user.description
	.card-footer
		-# .far.fa-calendar
		Signed up
		= time_ago_in_words(user.created_at)
		ago
		.card-footer
			= 'sign_in_count'.humanize
			\: 
			= user.sign_in_count
			.row
			= 'current_sign_in_at'.humanize
			\: 
			= user.current_sign_in_at
			.row
			= 'last_sign_in_at'.humanize
			\: 
			= user.last_sign_in_at
			.row
			= 'current_sign_in_ip'.humanize
			\: 
			= user.current_sign_in_ip
			.row
			= 'last_sign_in_ip'.humanize
			\: 
			= user.last_sign_in_ip
		.card-footer
			Confirmed email?
		- if user.confirmed_at.present?
			.badge.badge-success Yes
		- else
			.badge.badge-danger No
	-# 	.row
	-# 	.fa.fa-chalkboard-teacher
	-# 	= user.user
	-# .card-footer
	-# 	%td= link_to 'Show', user, class: 'btn btn-sm btn-primary'
	-# 	%td= link_to 'Edit', edit_user_path(user), class: 'btn btn-sm btn-warning'
	-# 	%td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger'
